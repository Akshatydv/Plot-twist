node_modules
.next
out
.env*
!.env.example
.DS_Store
*.tsbuildinfo
next-env.d.ts

# local application submissions (no database provisioned yet)
.data

# supabase local/CLI state (never commit)
supabase/.temp
supabase/.branches

# deploy platform caches
.netlify
.vercel

# OS/editor cruft
Thumbs.db
*.log

# generated deliverables — regenerate with make-invite.js / make-card.js /
# make-invite-cards.js and the pdf-source.html print step. Sources are tracked,
# outputs are not: they are large, binary, and rebuilt on every copy change.
invites/
dm-cards/
cards/
Plot-Twist-*.pdf

# scratch reference pulled out of the old Mac-built invite
_invite-ref/
